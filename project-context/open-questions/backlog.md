# Open Questions

| Priority | Question | Why It Matters | Source | Owner |
| --- | --- | --- | --- | --- |
| High | What is the current baseline for task creation time, administrative effort, sprint throughput, and visibility before the new solution is introduced? | Without a baseline, the 20% improvement target cannot be validated credibly. | Calidad document shared by user | TBD |
| High | Is this platform itself the source of truth for projects and tasks, or does it need to sync with an external tool such as Jira, Trello, or another Oracle-managed system? | Integration scope, data model, reporting boundaries, and migration effort all depend on this answer. | Calidad and system requirements documents shared by user | TBD |
| Medium | What exact fields define a "complete" task, and who decides whether a task is accepted without changes or corrected manually? | Several KPIs depend on these operational definitions. | Calidad document shared by user | TBD |
| High | How will authentication work across the portal and Telegram, including login, sign up, and mapping a Telegram identity to a platform user session? | The chatbot cannot safely execute role-based actions without a clear identity and session model. | System requirements document shared by user | TBD |
| High | What permissions can administrators grant or revoke, and what exact actions are developers allowed to perform beyond viewing tasks and changing status? | The source confirms roles but does not define the full authorization model. | System requirements document shared by user | TBD |
| Medium | Which chatbot commands are the official interface, and what is the final command syntax for editing tasks and creating projects? | The current document contains inconsistent command names and formats. | System requirements document shared by user | TBD |
| High | How should project membership actually be modeled if developers must access one or more projects, but the shared SQL currently shows only a single `CODIGOACCESO` on `PROYECTOS` and no explicit membership table? | Portal permissions and project visibility depend on this missing relationship. | System requirements and SQL schema shared by user | TBD |
| Medium | Which KPIs will be shown in the dashboard, how are they calculated, and what date filters or periods are supported? | Dashboard and reporting requirements are present, but the KPI catalog is still underspecified. | System requirements document shared by user | TBD |
| Medium | What is the minimum required test coverage for the currently untested features such as portal edit/delete flows, project creation, access codes, status updates, and KPI dashboards? | The current test set covers only part of the documented requirements, so release confidence would otherwise be uneven. | Test cases document shared by user | TBD |
| Medium | Which tests are expected to be manual versus automated, and what environment will be the official QA target for portal, API, bot, and notifications? | Execution strategy affects tooling, ownership, repeatability, and delivery readiness. | Test cases document shared by user | TBD |
| Medium | Which layer should be considered authoritative for time tracking: the application, the Telegram bot, or the Oracle triggers that set `FECHA_INICIO_REAL`, `FECHA_FIN_REAL`, and `HORAS_REALES`? | The SQL already automates part of the lifecycle, so the app must avoid conflicting calculations. | SQL schema shared by user | TBD |
| Medium | Are the currently defined SQL KPI queries the official first version of the dashboard, or do we still need to implement the broader metrics from the quality document before calling the reporting layer complete? | The SQL covers only a subset of the previously listed metrics. | Calidad and SQL schema shared by user | TBD |
